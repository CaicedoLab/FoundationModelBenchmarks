# Codebase benchmarking three scores: CHAMMI, Jump-CP, and HPA score


Works for three models: DINOv1, MAE, and SimCLR

To use files within the FoundationModels directory, use `git submodule update --init --recursive` to download that repository. Make sure to keep it up to date as changes get made there.