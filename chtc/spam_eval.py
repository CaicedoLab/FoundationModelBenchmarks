import argparse
import os
import subprocess
import numpy as np

ROOT_DIR = '/hdd/projects'

def main():
    
    
        
        # result = subprocess.run(
        #             f"condor_submit wandb_key={os.environ.get('WANDB_API_KEY')} checkpoint={} condor_eval.sh",
        #             shell=True,
        #             capture_output=True,
        #             check=True,
        #             text=True
        #         )

if __name__ == "__main__":
    main()