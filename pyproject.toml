[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "CHAMMI"
version = "0.1.0"

[project.scripts]
extract = "feature_extraction:main"
score = "benchmark:main"

[tool.setuptools.packages.find]
where = ["./morphem"]